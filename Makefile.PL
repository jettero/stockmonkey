# vi:set syntax=perl:

use ExtUtils::MakeMaker;

WriteMakefile(
    NAME         => 'Math::Business',
    DISTNAME     => 'stockmonkey',
    VERSION      => '2.2',
    PREREQ_PM    => {version=>0},
);
