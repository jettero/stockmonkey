# vi:set syntax=perl:

use ExtUtils::MakeMaker;

WriteMakefile(
    NAME         => 'Math::Business',
    DISTNAME     => 'stockmonkey',
    VERSION      => '1.0',
    PREREQ_PM    => {version=>0},
);
